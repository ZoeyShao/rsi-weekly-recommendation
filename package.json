{
  "name": "rsi-weekly-recommendation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A thin public gateway for OMA-powered weekly RSI paper reports.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local --watch src/server.js",
    "start": "node --env-file-if-exists=.env.local src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@hono/node-server": "2.1.1",
    "hono": "4.13.8",
    "marked": "18.0.13",
    "sanitize-html": "2.17.7"
  }
}
